<% provide(title: @post.title) %>

<!--Blog post-->
<div>
  <!--Post title-->
  <h2>
    <%= @post.title %>
    <% if logged_in? && logged_in_user?(@user) %>
      <!--Post edit button-->
      <%= link_to "Edit post", edit_post_path(@post), class: "btn btn-warning btn-xs" %>
    <% end %>
  </h2>

  <p>by <%= link_to @user.name, user_path(@user) %></p>
  <!--Post content-->
  <p><%= simple_format(@post.content) %></p>
</div>